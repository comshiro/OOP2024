#include "Repo.h"
#include <vector>
#include "Shape.h"

Shape* Repo::GetShape(string shapeType, string color) {
	
	Shape* s(shape)
}
void Repo::Add(Shape*);
void Repo::Remove(string info);
void Repo::PrintAll();